# 環境変数（Webhook URL などの秘匿情報を含むため追跡しない）
.env

# ビルド生成物
*.exe
/check-github-incidents

# テストの生成物
*.test
*.out

# 実行時に生成される通知済み状態（-local 用は別ファイル）
/notified_incidents.json
/notified_incidents.local.json

# 実行時に生成されるログ
/logs/*.log

# 実行時に生成される多重起動防止用のロック
/logs/*.lock
